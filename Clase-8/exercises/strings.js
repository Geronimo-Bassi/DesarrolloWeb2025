console.log("strings Exercise");

var palabra1 = "geronimobassi";

var palabramayuscula = palabra1.toUpperCase();

var palabrasubstring = palabra1.substring(0, 5);

var palabrasubstring2 = palabra1.substring(palabra1.length -3);

var palabrasubstring3 = palabra1.substring(0 , 1).toUpperCase() + palabra1.toLowerCase(1);

var posicionenblanco = palabra1.indexOf(" ");

var nuevostring = ("timeline outline")

var espacio = nuevostring.indexOf(" ");

var palabralarga =  nuevostring.substring(0,1).toUpperCase() + nuevostring.substring(1, espacio).toLocaleLowerCase() +  nuevostring.substring(espacio + 1, espacio + 2).toUpperCase() + nuevostring.substring(espacio + 2).toLocaleLowerCase();

